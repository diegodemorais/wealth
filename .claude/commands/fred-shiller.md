# fred-shiller — REDIRECT

> Substituído por CLI. Rodar:

```bash
~/claude/finance-tools/.venv/bin/python3 scripts/market_data.py --macro-us --factors
```

Macro US + FF5. Ver `scripts/market_data.py --help` para todas as opções.

$ARGUMENTS
