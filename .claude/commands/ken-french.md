# ken-french — REDIRECT

> Substituído por CLI. Rodar:

```bash
~/claude/finance-tools/.venv/bin/python3 scripts/market_data.py --factors
```

FF5 mensal via getfactormodels. Ver `scripts/market_data.py --help` para todas as opções.

$ARGUMENTS
