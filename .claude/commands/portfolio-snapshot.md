# portfolio-snapshot — REDIRECT

> Substituído por CLI. Rodar:

```bash
~/claude/finance-tools/.venv/bin/python3 scripts/ibkr_lotes.py --flex + market_data.py --etfs
```

Posições IBKR + preços. Ver `scripts/market_data.py --help` para todas as opções.

$ARGUMENTS
