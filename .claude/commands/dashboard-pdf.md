# [DEPRECATED] Dashboard PDF

> **Deprecated em 2026-04-08.** PDFs não são mais usados.
> O deploy Netlify foi absorvido pelo `/dashboard` (step 5).
> Scripts `scripts/dashboard_pdf.mjs` e `scripts/send_dashboard_email.py` mantidos para referência mas não são chamados.

Para publicar o dashboard: rodar `/dashboard` (inclui deploy Netlify automaticamente).
