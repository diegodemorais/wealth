# anbima-data — REDIRECT

> Substituído por CLI. Rodar:

```bash
~/claude/finance-tools/.venv/bin/python3 scripts/market_data.py --tesouro
```

Taxas NTN-B ANBIMA via pyield. Ver `scripts/market_data.py --help` para todas as opções.

$ARGUMENTS
