# macro-bcb — REDIRECT

> Substituído por CLI. Rodar:

```bash
~/claude/finance-tools/.venv/bin/python3 scripts/market_data.py --macro-br
```

Snapshot macro Brasil. Ver `scripts/market_data.py --help` para todas as opções.

$ARGUMENTS
