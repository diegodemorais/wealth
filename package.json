{
  "name": "wealth-dashboard",
  "version": "1.0.0",
  "description": "Browser automation for dashboard responsive testing",
  "scripts": {
    "test:responsive": "node scripts/test_responsive.js",
    "validate-data": "python3 scripts/validate_data.py",
    "test:pre-commit": "cd react-app && npm run test:ci && cd .. && npm run validate-data",
    "test:python": "python3 -m pytest scripts/tests/ -q",
    "test:integration": "python3 scripts/generate_data.py && npm run test:python && npm run validate-data"
  },
  "devDependencies": {
    "@playwright/test": "^1.59.1",
    "bcryptjs": "^3.0.3"
  },
  "dependencies": {
    "@radix-ui/react-progress": "^1.1.8",
    "@tailwindcss/postcss": "^4.2.2",
    "puppeteer": "^24.41.0"
  }
}
